# Minimal requirements for percell on iMac Catalina
# Excludes GUI dependencies (napari, PyQt5) which require Qt build tools

# Core scientific computing
numpy>=1.24.2
pandas>=1.3.3
scipy>=1.10.1
scikit-learn>=1.0.0
scikit-image>=0.19.1
matplotlib>=3.4.0

# Image processing
opencv-python>=4.5.0
tifffile>=2022.7.28
pillow>=9.0.0
read-roi>=1.6.0

# Utilities
tqdm>=4.62.0
natsort>=8.0.0
python-json-logger>=2.0.0
colorama>=0.4.4
alive-progress>=3.3.0

# Note: Cellpose is called from conda environment, not installed here
# Note: Napari and PyQt dependencies excluded for compatibility with older macOS
